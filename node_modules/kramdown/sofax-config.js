/*!
 * template、context 两项是必须配置的
 */

module.exports = {
  // 直接 `svn checkout` 下来的 vm 模板目录
  // 对应 SVN 地址：http://svnhz.alipay.net/svn/系统名/branches/分支名/htdocs/templates
  template: 'path/to/templates',

  // Mock 的数据全在这儿
  // 文件存放路径和模板路径对应
  context: 'path/to/context',

  // js、css、image、font，all in here
  // 可以是纯静态资源目录
  // 也可以是一个 spm 包目录
  // 也可以在该目录下存放多个 spm 包目录
  static: 'path/to/static',

  // 如果使用了 #cmsparse，需要先拉回本地哦，亲
  // 对应 SVN 地址：http://svnhz.alipay.net/svn/uisvr/trunk/
  cms: 'path/to/cms',

  // 这里是公共 uisvr，跟 cms 文件一样，也需要拉回本地呢
  // 对应 SVN 地址：http://sources.alipay.net/svn/ecmngsvn/trunk/alipay/ecmng/
  uisvr: 'path/to/uisvr',

  // 写 UI 测试了？那就放这里吧
  // 文件存放路径和模板路径对应
  test: 'path/to/test',

  // 配置文件编码，默认 utf-8，你可能需要 gbk 什么的
  charset: 'utf-8',

  // 如果项目支持多语言，可以指定地区码，默认 en_US
  locale: 'en_US',

  // 本地 HTTP Server 的端口号，默认 7777。我会说这是沉鱼定的么？
  port: 7777
}
